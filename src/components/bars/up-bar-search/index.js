import React from 'react'

import { Box } from '@mui/material'
import { styled } from '@mui/system'

const Root = styled(Box)(({ theme }) => ({
    background: 'white',
    borderRadius: 16,
    height: 88,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    width: '69%'
}))

const UpBarSearch = () => {
    return (
        <Root>

        </Root>
    )
}

export default UpBarSearch